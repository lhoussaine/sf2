{% extends "HbBlogBundle::layout.html.twig" %}

{% block title %}
    {{ parent() }} - Modifier Commentaire
{% endblock %}
{% block blog %}
    <h3>Formulaire de modification de commentaire</h3>
    <div class="well">
        <form method="post" {{ form_enctype(form) }}>
            {{ form_widget(form) }}
            <input type="submit" class="btn btn-primary" />
        </form>
    </div>
            <p><a href="{{ path('blogbundle_Articlevoir', {'id': commentaire.article.id }) }}">Retour à l'article</a></p>
{% endblock %}

